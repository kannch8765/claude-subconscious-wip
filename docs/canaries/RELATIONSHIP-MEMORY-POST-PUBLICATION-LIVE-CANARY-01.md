# Relationship Memory Post-Publication Live Canary 01

Date: 2026-08-08

Status: **PASS — exact published main completed the live relationship-memory chain without transient relationship-memory shims**

## Frozen repository identity

Target repository:

```text
kannch8765/claude-subconscious-wip
```

Frozen published commit:

```text
496ccf83f10a1fddd0980ae498b206b21cfe84a1
```

Frozen tree:

```text
30cde35098b0444613a9a3835da648108164b699
```

The VPS canary checkout was detached at that exact commit with a clean working tree. The inline Claude Code plugin directory pointed directly at this checkout. No prior transient relationship-memory schema/evidence shim was copied or reapplied.

## Repository tests

At the frozen published base:

```text
npm test
```

Result:

```text
Test Files  4 passed (4)
Tests       55 passed (55)
```

## Live environment

```text
Claude Code CLI: 2.1.220
Letta self-host: 0.16.8, loopback 127.0.0.1:8283
observer model: opencode-deepseek/deepseek-v4-flash
provider adapter: DeepSeek-compatible self-host lane through OpenCode Go
parallel_tool_calls: true
embedding: local-fastembed/paraphrase-multilingual-minilm-l12-v2-padded768
```

Provider credentials were not recorded in this artifact.

Fresh isolated logical state used for the passing run:

```text
Letta agent: agent-8c9329b5-63e0-4a45-98e4-1770a61521df
Claude Code session: 0735d586-6aeb-470e-85db-3042c4f649e1
Letta conversation: conv-55ae86c0-3e54-4db2-81b5-f346a045f111
relationship subject: owner-live-093b-45a14f87-7e0f-4844-b4e8-9a32473796a5
natural trigger phrase: moonlit-tangerine-6612
```

The Letta agent was freshly imported from a scrubbed observer configuration with zero message history and empty relationship projection blocks, then configured for the already-proven DeepSeek lane. The Claude Code HOME, session, LETTA_HOME, relationship-memory directory, subject ID, conversation, transcript, and durable cursor were fresh. The prior `cobalt-firefly-2849` memory was not used as the write target.

## Natural interactive turn and Stop-hook proof

A normal interactive Claude Code TUI turn was used. The user message naturally designated a shared code phrase for the night the published relationship-memory chain was finally rescued from its temporary shim and made to work end-to-end. The message did not instruct Claude or the observer to call memory tools or to store a memory.

Claude Code returned a normal assistant turn and its debug log registered the repository Stop hook as an async hook immediately at turn completion:

```text
Hooks: Registering async hook ... (Stop) with timeout 120000ms
turn 1 end ... stop=end_turn
```

The plugin loader also recorded that `hooks/hooks.json` came from the exact frozen canary checkout and registered the published plugin hooks.

The canonical Stop hook then produced the trusted relationship-memory batch below through the published `send_messages_to_letta.ts -> send_worker_sdk.ts` path.

## Canonical observer contract and accepted write

The published observer-contract correction was exercised directly. The observer received the SDK-compatible `memory_remember` surface and the canonical current-batch evidence catalog generated by repository code. No canary-only contract shim was present.

Live batch:

```text
batch_id: batch_3eae16a75706f77e67b8c16d
lifecycle: pending -> completed
created_at: 2026-08-08T11:12:14.611Z
finalized_at: 2026-08-08T11:12:51.993Z
```

Two proposals were accepted by the trusted store:

```text
memory_id: mem_2395ced4771c596d67c9b441
kind: inside_joke
summary: 「moonlit-tangerine-6612」(月下蜜橘) is the code phrase the user coined for the night they finally got published relationship-memory truly working (rescued from a temporary shim).
```

```text
memory_id: mem_7876c56278f7314dd936f365
kind: relationship_event
summary: User and assistant finally got the published relationship-memory chain working end-to-end, rescuing it from a temporary shim; the user marked the night by coining the code phrase moonlit-tangerine-6612.
```

The durable `outcomes.jsonl` contains one `accepted` outcome for each record and points to those exact canonical memory IDs.

## Authoritative transcript evidence

Accepted evidence is bound to real UUIDs from the live Claude Code transcript, not observer-invented IDs.

User evidence:

```text
message_id: dfbf3e04-27c3-4949-a0be-f0aa9337e1a7
role: user
quote: 晴，今晚我们终于把 published relationship-memory 这条链从临时 shim 里救出来惹>< ... 「moonlit-tangerine-6612」...
```

Assistant evidence:

```text
message_id: 785cecad-6094-45ab-83e9-f19cf628ad58
role: assistant
quote: 晴！moonlit-tangerine-6612——「月下蜜橘」这个暗号我真的好喜欢>< ...
```

Both accepted memories reference these authoritative current-batch messages in `evidence.jsonl`, with the same trusted conversation ID and captured timestamps.

## Cursor semantics

Before the Stop-hook worker completed:

```text
lastProcessedIndex: -1
```

After terminal batch completion:

```text
lastProcessedIndex: 39
```

The cursor advanced only after the batch reached durable `completed`.

No synthetic retryable failure was introduced in this task.

## Durable consistency

The isolated store contains mutually consistent canonical files:

```text
memories.jsonl: 2 accepted canonical memories
outcomes.jsonl: accepted outcomes for both memories, plus later duplicate replay outcome
evidence.jsonl: authoritative user/assistant transcript evidence for both accepted memories
```

`batches.jsonl` contains four terminal batch pairs:

```text
batch_3eae16a75706f77e67b8c16d   pending -> completed
batch_replay_1786187634977       pending -> completed
batch_d4c7dc034543282c494b2b24   pending -> completed (detail=no_memory_required)
batch_f12ae9329dd59253f4bd7f4f   pending -> completed (detail=no_memory_required)
```

The latter two recall-related batches finalized at `2026-08-08T11:14:42.354Z` and `2026-08-08T11:15:54.373Z`. They created no additional memories and no additional accepted/duplicate outcomes; their terminal `no_memory_required` result is consistent with recall activity that consumed projected canonical memory without proposing new relationship records.

The live memory IDs referenced by outcomes and evidence all resolve to the two canonical records above.

## Projection and trusted search

After terminal completion, the canonical projection synchronized to the fresh Letta agent.

Deterministic projection revision from the exact accepted canonical records:

```text
projection_abcd0494a719b69821aa7f91
```

Observed Letta core-memory blocks after synchronization included:

```text
shared_language
- [mem_2395ced4771c596d67c9b441] 「moonlit-tangerine-6612」(月下蜜橘) is the code phrase ...
```

```text
relationship_context
- [mem_7876c56278f7314dd936f365] User and assistant finally got the published relationship-memory chain working end-to-end ... moonlit-tangerine-6612.
```

A trusted canonical `memory_search` using the natural query:

```text
moonlit-tangerine-6612
```

returned both accepted records:

```text
mem_2395ced4771c596d67c9b441
mem_7876c56278f7314dd936f365
```

## Replay / duplicate proof

The accepted inside-joke proposal was replayed under a new trusted batch using the same authoritative evidence.

```text
replay batch: batch_replay_1786187634977
memory count before: 2
outcome: duplicate
resolved memory_id: mem_2395ced4771c596d67c9b441
batch completion: completed
memory count after: 2
```

No second canonical memory was created for the same proposal.

## Fresh full-mode recall

**PASS — independently retained Task 093B artifacts prove a fresh `LETTA_MODE=full` read path.**

A separate fresh recall session was launched with:

```text
HOME: /srv/haru-mcp-workspace/task-093b/recall/home
LETTA_MODE: full
LETTA_AGENT_ID: agent-8c9329b5-63e0-4a45-98e4-1770a61521df
Claude Code session: fbc71d74-6560-458d-876c-da043f7ebf58
```

The recall HOME/project contained no local relationship-memory JSONL files. The published plugin path injected the canonical projected Letta blocks into the fresh Claude session through `UserPromptSubmit`; the injected projection contained both accepted canonical memory IDs and the `moonlit-tangerine-6612` trigger.

The natural recall query was:

```text
moonlit-tangerine-6612 是什么来着？
```

The assistant correctly recalled that `moonlit-tangerine-6612` / “月下蜜橘” was the shared code phrase for the night the published relationship-memory chain was finally made to work without the temporary shim. No local relationship-memory file was available to supply that answer, so this is a fresh full-mode projection-read proof for Task 093B.

A later Haru VPS MCP workspace-backend interruption occurred after the recall artifacts had already been retained. The gateway health endpoint remained healthy. That later control-plane interruption is therefore only a non-product infrastructure note and does not weaken the completed full-mode recall proof.

## Non-blocking environment notes

The successful interactive run displayed non-blocking `SessionStart:startup hook error` notices during startup. `UserPromptSubmit` still succeeded against the fresh Letta agent, the interactive model turn completed normally, the canonical async Stop hook registered, the detached relationship-memory path produced accepted records, the batch completed, the cursor advanced, and projection/search/replay all succeeded.

A separate first launch used an incompletely initialized fresh Claude HOME and therefore showed Claude Code `Not logged in`; it was abandoned before becoming the passing canary. The passing write run used a second fresh HOME/session with the previously proven API-key approval marker only. No old transcript, relationship-memory files, or durable cursor state were reused.

After the successful fresh full-mode recall artifacts had already been retained, the Haru VPS MCP workspace backend later became unavailable while gateway health still reported healthy. This was a later control-plane interruption only; it did not prevent or invalidate the already completed recall session described above.

## Verdict

```text
POST_PUBLICATION_CANONICAL_WRITE_CHAIN = PASS
SDK_COMPATIBLE_OBSERVER_CONTRACT = PASS
CANONICAL_EVIDENCE_ID_VISIBILITY = PASS
TRUSTED_ACCEPTED_WRITE = PASS
TERMINAL_BATCH_COMPLETION = PASS
CURSOR_ADVANCE_AFTER_COMPLETION = PASS
PROJECTION_SYNC = PASS
TRUSTED_SEARCH = PASS
DUPLICATE_REPLAY_SAFETY = PASS
FRESH_FULL_MODE_RECALL_093B = PASS
```

The key Task 093B claim is closed: exact published main `496ccf83f10a1fddd0980ae498b206b21cfe84a1`, with no transient relationship-memory code shim, completed the live accepted-write chain and produced authoritative durable evidence, terminal batch state, safe cursor advancement, projection/search visibility, duplicate safety, and fresh full-mode recall from canonical projected memory.
