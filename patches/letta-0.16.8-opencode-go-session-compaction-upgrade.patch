diff --git a/letta/agents/letta_agent_v3.py b/letta/agents/letta_agent_v3.py
index d9ab48a..e4abbc6 100644
--- a/letta/agents/letta_agent_v3.py
+++ b/letta/agents/letta_agent_v3.py
@@ -2127,6 +2127,7 @@ class LettaAgentV3(LettaAgentV2):
             trigger_threshold=trigger_threshold,
             run_id=run_id,
             step_id=step_id,
+            opencode_session_id=self.conversation_id or self.agent_state.id,
             use_summary_role=use_summary_role,
             trigger=trigger,
             context_tokens_before=context_tokens_before,
diff --git a/letta/services/summarizer/compact.py b/letta/services/summarizer/compact.py
index 025f624..912ad96 100644
--- a/letta/services/summarizer/compact.py
+++ b/letta/services/summarizer/compact.py
@@ -147,6 +147,7 @@ async def compact_messages(
     trigger_threshold: Optional[int] = None,
     run_id: Optional[str] = None,
     step_id: Optional[str] = None,
+    opencode_session_id: Optional[str] = None,
     use_summary_role: bool = True,
     trigger: Optional[str] = None,
     context_tokens_before: Optional[int] = None,
@@ -168,6 +169,7 @@ async def compact_messages(
         trigger_threshold: If provided, verify context stays below this after compaction.
         run_id: Optional run ID for telemetry.
         step_id: Optional step ID for telemetry.
+        opencode_session_id: Stable conversation ID for OpenCode Go routing during compaction.
         use_summary_role: If True, create summary message with role=summary.
         trigger: What triggered the compaction (for stats).
         context_tokens_before: Token count before compaction (for stats).
@@ -202,6 +204,7 @@ async def compact_messages(
                 compaction_settings=summarizer_config,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 timezone=timezone,
                 agent_tags=agent_tags,
                 tools=tools,
@@ -227,6 +230,7 @@ async def compact_messages(
                     compaction_settings=fallback_config,
                     run_id=run_id,
                     step_id=step_id,
+                    opencode_session_id=opencode_session_id,
                     timezone=timezone,
                     agent_tags=agent_tags,
                     tools=tools,
@@ -250,6 +254,7 @@ async def compact_messages(
                     agent_tags=agent_tags,
                     run_id=run_id,
                     step_id=step_id,
+                    opencode_session_id=opencode_session_id,
                     billing_context=billing_context,
                 )
                 summarization_mode_used = "all"
@@ -266,6 +271,7 @@ async def compact_messages(
                 compaction_settings=summarizer_config,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 timezone=timezone,
                 agent_tags=agent_tags,
                 tools=tools,
@@ -291,6 +297,7 @@ async def compact_messages(
                 agent_tags=agent_tags,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 billing_context=billing_context,
             )
             summarization_mode_used = "all"
@@ -304,6 +311,7 @@ async def compact_messages(
             agent_tags=agent_tags,
             run_id=run_id,
             step_id=step_id,
+            opencode_session_id=opencode_session_id,
             billing_context=billing_context,
         )
     elif summarizer_config.mode == "sliding_window":
@@ -318,6 +326,7 @@ async def compact_messages(
                 agent_tags=agent_tags,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 billing_context=billing_context,
             )
         except ContextWindowExceededError:
@@ -341,6 +350,7 @@ async def compact_messages(
                 agent_tags=agent_tags,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 billing_context=billing_context,
             )
             summarization_mode_used = "all"
@@ -376,6 +386,7 @@ async def compact_messages(
                 agent_tags=agent_tags,
                 run_id=run_id,
                 step_id=step_id,
+                opencode_session_id=opencode_session_id,
                 billing_context=billing_context,
             )
             summarization_mode_used = "all"
diff --git a/letta/services/summarizer/self_summarizer.py b/letta/services/summarizer/self_summarizer.py
index b411af2..46b0362 100644
--- a/letta/services/summarizer/self_summarizer.py
+++ b/letta/services/summarizer/self_summarizer.py
@@ -33,6 +33,7 @@ async def self_summarize_all(
     timezone: str,
     run_id: Optional[str] = None,
     step_id: Optional[str] = None,
+    opencode_session_id: Optional[str] = None,
     agent_tags: Optional[List[str]] = None,
     # For cache compatibility with regular agent requests
     tools: Optional[List[dict]] = None,
@@ -121,7 +122,13 @@ async def self_summarize_all(
     from letta.services.summarizer.summarizer import _run_summarizer_request
 
     try:
-        summary_text = await _run_summarizer_request(request_data, messages_with_request, agent_llm_config, llm_client)
+        summary_text = await _run_summarizer_request(
+            request_data,
+            messages_with_request,
+            agent_llm_config,
+            llm_client,
+            opencode_session_id=opencode_session_id,
+        )
     except Exception as e:
         logger.error(f"Self-summarization request failed: {e}")
 
@@ -163,6 +170,7 @@ async def self_summarize_sliding_window(
     timezone: str,
     run_id: Optional[str] = None,
     step_id: Optional[str] = None,
+    opencode_session_id: Optional[str] = None,
     agent_tags: Optional[List[str]] = None,
     # For cache compatibility with regular agent requests
     tools: Optional[List[dict]] = None,
@@ -253,6 +261,7 @@ async def self_summarize_sliding_window(
         timezone=timezone,
         run_id=run_id,
         step_id=step_id,
+        opencode_session_id=opencode_session_id,
         agent_tags=agent_tags,
         tools=tools,
         billing_context=billing_context,
diff --git a/letta/services/summarizer/summarizer.py b/letta/services/summarizer/summarizer.py
index d7945b8..2f0016a 100644
--- a/letta/services/summarizer/summarizer.py
+++ b/letta/services/summarizer/summarizer.py
@@ -15,6 +15,7 @@ from letta.constants import (
 from letta.errors import ContextWindowExceededError, LLMProviderOverloaded, LLMRateLimitError
 from letta.helpers.message_helper import convert_message_creates_to_messages
 from letta.llm_api.llm_client import LLMClient
+from letta.llm_api.openai_client import with_opencode_go_session_header
 from letta.log import get_logger
 from letta.otel.tracing import trace_method
 from letta.schemas.enums import AgentType, LLMCallType, MessageRole, ProviderCategory, ProviderType
@@ -496,6 +497,7 @@ async def simple_summary(
     agent_tags: List[str] | None = None,
     run_id: str | None = None,
     step_id: str | None = None,
+    opencode_session_id: str | None = None,
     compaction_settings: dict | None = None,
     billing_context: BillingContext | None = None,
 ) -> str:
@@ -560,7 +562,7 @@ async def simple_summary(
 
     request_data = llm_client.build_request_data(AgentType.letta_v1_agent, input_messages_obj, summarizer_llm_config, tools=[])
     try:
-        summary = await _run_summarizer_request(request_data, input_messages_obj, summarizer_llm_config, llm_client, actor=actor)
+        summary = await _run_summarizer_request(request_data, input_messages_obj, summarizer_llm_config, llm_client, actor=actor, opencode_session_id=opencode_session_id)
     except Exception as e:
         # handle LLM error (likely a context window exceeded error)
         try:
@@ -598,7 +600,7 @@ async def simple_summary(
             )
 
             try:
-                summary = await _run_summarizer_request(request_data, input_messages_obj, summarizer_llm_config, llm_client, actor=actor)
+                summary = await _run_summarizer_request(request_data, input_messages_obj, summarizer_llm_config, llm_client, actor=actor, opencode_session_id=opencode_session_id)
             except Exception as fallback_error_a:
                 # Fallback B: hard-truncate the user transcript to fit a conservative byte budget.
                 # We use bytes (not chars) to be more applicable across languages.
@@ -639,7 +641,12 @@ async def simple_summary(
                 )
                 try:
                     summary = await _run_summarizer_request(
-                        request_data, input_messages_obj, summarizer_llm_config, llm_client, actor=actor
+                        request_data,
+                        input_messages_obj,
+                        summarizer_llm_config,
+                        llm_client,
+                        actor=actor,
+                        opencode_session_id=opencode_session_id,
                     )
                 except Exception as fallback_error_b:
                     logger.error(f"Transcript truncation fallback also failed: {fallback_error_b}. Propagating error.")
@@ -746,6 +753,7 @@ async def _run_summarizer_request(
     llm_client: LLMClient,
     actor: User | None = None,
     is_fallback: bool = False,
+    opencode_session_id: str | None = None,
 ) -> str:
     """Run summarization request and return assistant text.
 
@@ -758,7 +766,9 @@ async def _run_summarizer_request(
     """
 
     try:
-        return await _execute_summarizer_request(req_data, req_messages_obj, llm_config, llm_client)
+        return await _execute_summarizer_request(
+            req_data, req_messages_obj, llm_config, llm_client, opencode_session_id=opencode_session_id
+        )
     except Exception as e:
         if not isinstance(e, (LLMProviderOverloaded, LLMRateLimitError)):
             handled = llm_client.handle_llm_error(e, llm_config=llm_config)
@@ -809,7 +819,13 @@ async def _run_summarizer_request(
 
             fallback_req = fallback_client.build_request_data(AgentType.letta_v1_agent, req_messages_obj, fallback_llm_config, tools=[])
             return await _run_summarizer_request(
-                fallback_req, req_messages_obj, fallback_llm_config, fallback_client, actor=actor, is_fallback=True
+                fallback_req,
+                req_messages_obj,
+                fallback_llm_config,
+                fallback_client,
+                actor=actor,
+                is_fallback=True,
+                opencode_session_id=opencode_session_id,
             )
         except Exception as fallback_error:
             logger.warning(f"Summarization fallback failed: {fallback_error}, re-raising original error")
@@ -817,9 +833,17 @@ async def _run_summarizer_request(
 
 
 @trace_method
-async def _execute_summarizer_request(req_data: dict, req_messages_obj: list[Message], llm_config: LLMConfig, llm_client: LLMClient) -> str:
+async def _execute_summarizer_request(
+    req_data: dict,
+    req_messages_obj: list[Message],
+    llm_config: LLMConfig,
+    llm_client: LLMClient,
+    opencode_session_id: str | None = None,
+) -> str:
     """Execute the actual LLM request for summarization."""
 
+    req_data = with_opencode_go_session_header(req_data, llm_config, opencode_session_id)
+
     if llm_config.model_endpoint_type in [ProviderType.anthropic, ProviderType.bedrock]:
         logger.info(
             "Summarizer: using provider streaming (%s/%s) to avoid long-request failures",
diff --git a/letta/services/summarizer/summarizer_all.py b/letta/services/summarizer/summarizer_all.py
index 3e0ee71..8d896f3 100644
--- a/letta/services/summarizer/summarizer_all.py
+++ b/letta/services/summarizer/summarizer_all.py
@@ -27,6 +27,7 @@ async def summarize_all(
     agent_tags: Optional[List[str]] = None,
     run_id: Optional[str] = None,
     step_id: Optional[str] = None,
+    opencode_session_id: Optional[str] = None,
     billing_context: Optional[BillingContext] = None,
 ) -> str:
     """
@@ -71,6 +72,7 @@ async def summarize_all(
         agent_tags=agent_tags,
         run_id=run_id,
         step_id=step_id,
+        opencode_session_id=opencode_session_id,
         compaction_settings={
             "mode": "summarize_all",
             "clip_chars": summarizer_config.clip_chars,
diff --git a/letta/services/summarizer/summarizer_sliding_window.py b/letta/services/summarizer/summarizer_sliding_window.py
index a04f70c..aaf2e04 100644
--- a/letta/services/summarizer/summarizer_sliding_window.py
+++ b/letta/services/summarizer/summarizer_sliding_window.py
@@ -110,6 +110,7 @@ async def summarize_via_sliding_window(
     agent_tags: Optional[List[str]] = None,
     run_id: Optional[str] = None,
     step_id: Optional[str] = None,
+    opencode_session_id: Optional[str] = None,
     billing_context: Optional[BillingContext] = None,
 ) -> Tuple[str, List[Message]]:
     """
@@ -212,6 +213,7 @@ async def summarize_via_sliding_window(
         agent_tags=agent_tags,
         run_id=run_id,
         step_id=step_id,
+        opencode_session_id=opencode_session_id,
         compaction_settings={
             "mode": "sliding_window",
             "messages_summarized": len(messages_to_summarize),
