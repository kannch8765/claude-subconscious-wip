# Investigation 02：可编辑配置与第一批清理边界

这是本分支的任务入口。收到分支名后，请先读本文件，完成调查并将报告提交回同一分支，供主窗口 orchestrator 晴讨论。此单授权调查和报告提交，尚未授权实现清理或配置迁移。

## 工作位置

- 仓库：kannch8765/claude-subconscious-wip
- 工作及报告分支：investigation/02-config-and-cleanup
- 源码调查基准：e83e75956b468dc43491bcb8fafff8ac70d0e854
- 报告位置：docs/INVESTIGATION-02-CONFIG-AND-CLEANUP.md
- 第一份结构报告：investigation/structure-map-e83e759 分支，报告 commit e11eb1392236a23b0c04947ca00f9b8a7964f7e6，文件 docs/INVESTIGATION-01-CURRENT-STRUCTURE-MAP.md。
- 先确认适用仓库说明和当前分支状态。调查基准保持固定；如发现相关新变动，单独说明，不自动合并 main 或其他分支。保留别人的提交。

## 用户背景和这次想解决的事

仓库经历了很多 PR、debug 和故障修复。用户希望先整理出容易维护的代码，再继续功能实验。用户观察到 prompt 和 model 的改动常常需要直接改源码，想知道能否改成编辑 MD / JSON 等文件后生效，但没有指定必须采用某种格式或编译方案。

重要背景：
- sync 目前仍是实验阶段，追求低延迟，因此其工具与 async 不同。这种差异本身不是 bug，也不要求两条路径合并。
- 用户反馈显式 recall 目前不可用；原因尚未确认。本单只在涉及配置边界时标记它，不展开功能修复，也不据此删除模块。
- 主窗口负责总体决策；你负责定点查证和提出建议，不必通读全仓。

核心问题：哪些经常调整的内容适合移出源码？已有配置机制是否其实足够，只是分散或难用？怎样在不丢失既有保护的前提下，让第一批整理真正减少维护成本？

## 建议调查路径

从第一份报告入手，围绕 prompt/model 配置的定义、加载、覆盖、远端 reconciliation 定点阅读。可从 scripts/agent_config.ts、scripts/backfill_agent_config.ts、live worker、backfill 两个当前入口及其直接引用的 prompt/AgentFile/config 文件开始；具体范围以实际引用为准。

### 1. 画出“改一处内容，到实际生效”的配置地图

对 live async、实验 sync、普通 backfill、Omen backfill 分别确认：
- prompt 的原始来源：源码字符串、文件、AgentFile、远端保存值或其他；
- model/provider/context 等运行设置在哪里定义，已有 env/CLI/file 覆盖如何工作；
- 谁读取它，什么时候读取，怎样写入或同步到 Letta；
- 已存在 agent/conversation 与新建对象如何不同；修改后需重启、重建还是下次 reconcile 即生效；
- 同一项有没有多份真值或被后续 reconcile 覆盖的可能。

显式 recall 只需标明是否共享这些配置；独立部分留给后续调查。
做紧凑表格，保留关键文件、符号和证据。区分仓库默认、仓库支持的覆盖、未知的 production 设置。不要把“用户经常改源码”直接当成“没有配置能力”。

### 2. 探索最小的可编辑配置方案

比较适用于本仓库的两三个轻量选择，例如沿用现有配置、运行时读文件、构建时嵌入；推荐一个并解释取舍。MD/JSON 是候选，不是预设答案。若已有合适机制，优先说明如何收拢和使用它。

重点回答：
- 哪些是可编辑文案/策略，哪些属于必须由代码保证的 schema、工具权限和行为边界？
- prompt 中的动态插值、XML 包装、角色差异如何保留？避免用字符串拼接迁移引入转义或内容变化。
- 是否适合共享基础 prompt 加角色片段？只有实际重复证据支持时再建议，避免产生复杂模板框架。
- 配置 schema/类型、默认值、覆盖优先级、未知字段和缺失/非法文件如何处理？不能静默切换成另一个模型并产生费用。
- 密钥仍通过已有凭据渠道提供，配置文件只存非秘密值或引用。
- 文件如何随插件/发布包交付，路径如何在不同 cwd、安装方式及现有平台支持下定位？
- 文件变更怎样被现有远端 reconciliation 识别并生效？模型专属验证（如 Omen verified runtime）是否仍需保留？
- 如何用非敏感版本或指纹确认“实际使用的是哪份配置”，以及如何回退？

给一个足够说明结构的小示例即可，不生成完整配置框架或重写 prompt。未能从源码证明的地方明确列出。

### 3. 顺带找到第一批可以收拢的重复

限制在本次读到的配置与当前 backfill 入口附近，不扩大成全仓 gate 扫描：
- 对比 scripts/relationship_memory_backfill.ts 与 scripts/relationship_memory_backfill_omen.ts，区分共享流程、必要的模型差异与意外漂移。
- 对涉及的 gate 分类：必要保护、实验开关、历史兼容、疑似无效、未知。
- 每个删除/收拢候选都要说明调用证据、原本用途、仍需保留的行为以及验证方式。
- 外部调用无法确认时保留缺口；包脚本未注册不代表无调用。必要时追溯少量相关历史/文档，不扫描全部 PR。
- mutation lock、checkpoint、权限 preflight、embedding cooldown 等保护不能仅因复杂或重复而判定废弃。不要把任意安全 gate 都变成用户可关闭的配置。

## 回传成果

报告开头先给：
1. 当前是否真的需要改源码才能改 prompt/model；
2. 你推荐的最小配置方向；
3. 最值得先实施的一小块，以及暂缓项。

正文包含配置来源/覆盖/生效表、必要的调用关系、附近的去留候选和证据。最后提出最多 3 个有先后顺序的小实施单建议，分别说明范围、行为保持条件、相关离线测试和风险；不预设全部值得执行。

列出迁移时怎样验证 prompt 渲染等价、配置覆盖优先级、已有 agent 更新、第二次 reconcile 无额外修改、非法配置及打包文件缺失。标明哪些验证可离线，哪些必须留待后续授权的真实服务验证。静态推断不写成测试通过。

可以在安全的临时/mock 环境做必要的定点离线验证，先确认它不会调用实际服务或读取生产数据。本单不进入 VPS，不调用 Letta/模型/embedding，不启动 backfill，不修改运行源码、依赖、现有配置或测试，不创建 PR、不合并。

报告写到指定位置并 commit/push 回本分支。结束时只需交代分支、报告路径、报告 exact commit，以及最重要的 2–3 句结论。遇到权限阻断就说明缺口，不绕过权限。
